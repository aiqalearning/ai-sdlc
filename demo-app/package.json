{
  "name": "ai-sdlc-demo-app",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Express task app used as the AI-SDLC pipeline's runnable target.",
  "scripts": {
    "start": "node src/server.js",
    "test": "playwright test"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  },
  "engines": {
    "node": ">=20"
  }
}
